# Copernicus Climate Data Store
CDS_API_KEY=your_cds_api_key_here
CDS_API_URL=https://cds.climate.copernicus.eu/api/v2

# Google Earth Engine
GEE_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com
GEE_PRIVATE_KEY_PATH=/path/to/private-key.json

# ESA Copernicus Open Access Hub (optional, for Sentinel data)
SCIHUB_USERNAME=your_username
SCIHUB_PASSWORD=your_password

# AWS Credentials (for Sentinel data on AWS)
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_REGION=us-west-2

# Database (if using for intermediate storage)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agri_insurance
DB_USER=postgres
DB_PASSWORD=your_password
